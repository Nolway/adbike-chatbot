# Generated by Okty.io
version: '3'
services:
    adminer:
        image: 'adminer:4.7'
        ports:
            - '8081:8080'
    postgresql:
        image: 'postgres:10.4'
        ports:
            - '5234:5432'
        environment:
            - PGDATA=/var/lib/postgresql/data/pgdata
            - POSTGRES_DB=adbike
            - POSTGRES_USER=adbike
            - POSTGRES_PASSWORD=adbike
    php-fpm:
        build: docker/php
        volumes:
            - './src/:/usr/share/nginx/html'
    nginx:
        build: docker/nginx
        ports:
            - '80:80'
        volumes:
            - './src/:/usr/share/nginx/html'
